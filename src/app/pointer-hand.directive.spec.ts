import { PointerHandDirective } from './pointer-hand.directive';

describe('PointerHandDirective', () => {
  it('should create an instance', () => {
    const directive = new PointerHandDirective();
    expect(directive).toBeTruthy();
  });
});
